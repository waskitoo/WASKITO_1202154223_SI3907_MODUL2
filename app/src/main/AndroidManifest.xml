<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.razerblade.waskito_1202154223_modul2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Splash screen -->
        <activity
            android:name=".Splash"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".TakeAway"
            android:label="Take Away"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".DineIn"
            android:label="Dine In"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".DaftarMenu"
            android:label="Daftar Menu"/>
        <activity android:name=".MenuAdapter" />
        <activity android:name=".MenuDetail"
            android:parentActivityName=".DaftarMenu"></activity>
    </application>

</manifest>